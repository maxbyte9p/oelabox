---
kojid_service: "compile/koji.oelabox.local"
koji_web_service: "HTTP/koji.oelabox.local"
kojira_service: "kojira/koji.oelabox.local"
koji_gc_service: "koji-gc/koji.oelabox.local"

ipa_services:
  - "{{ kojid_service }}"
  - "{{ koji_web_service }}"
  - "{{ kojira_service }}"
  - "{{ koji_gc_service }}" 
